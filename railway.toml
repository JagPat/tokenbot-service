[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
watchPatterns = ["**/*.js", "package.json", "services/**", "routes/**", "config/**"]

[deploy]
startCommand = "npm start"
restartPolicyType = "always"
healthcheckPath = "/health"
healthcheckTimeout = 30

